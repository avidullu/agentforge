// Public AgentForge config API.
// Always exports the selected generated library. There is no conditional
// filesystem import — generated/app_config.selected.dart is always present
// (synthetic values in git; real values may rewrite the workspace copy only).
export 'generated/app_config.selected.dart';
